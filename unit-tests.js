function testInitialInput() {
	//if an initial board includes characters not considered valid, will the board return an error
}

function testNullBoard() {
	//does the program catch a null board? if null initial.example, or null initial.solution?
}

function testIncompleteInitialBoard() {
	//if not all the solutions are listed in the array - (or if there are invalid chars in solution) will the program catch it?
}

function catchBadInput() {
	//on keydown, if not an approved key, the sudoku board value at that spot should not be the value of the keycode from keydown
}

function